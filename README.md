# Unit Test Automator

Unit Test Automator is a Python tool that automatically generates **pytest** unit tests for your Python files. 
It uses the **Abstract Syntax Tree (AST)** to parse your code, extracts functions and classes, 
and generates comprehensive test cases using the **OpenAI API**.

